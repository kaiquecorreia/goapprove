import { PageLoadingState } from '@/components/ui/PageLoadingState';

export default function Loading() {
  return (
    <PageLoadingState title="OCs Pendentes" description="Ordens de compra aguardando aprovação." />
  );
}
