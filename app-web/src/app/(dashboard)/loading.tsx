import { PageLoadingState } from '@/components/ui/PageLoadingState';

export default function Loading() {
  return (
    <PageLoadingState title="Dashboard" description="Visão geral do fluxo de aprovação de OCs." />
  );
}
